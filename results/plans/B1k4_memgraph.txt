-- B1k4 (memgraph) 4-hop expansion (index-join chain vs. traversal)

MATCH (n:Concept {name: 'enzyme'})-[r1]->()-[r2]->()-[r3]->()-[r4]->(x:Concept)
WHERE r1.weight >= 0.5 AND r2.weight >= 0.5 AND r3.weight >= 0.5 AND r4.weight >= 0.5
RETURN DISTINCT x.name AS name
ORDER BY name LIMIT 100

---- PLAN ----
* None
